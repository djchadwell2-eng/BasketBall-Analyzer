/node_modules
/.next/
/out/
/build

.DS_Store
*.pem

npm-debug.log*
yarn-debug.log*
yarn-error.log*

.env*.local

# eval harness outputs — cached Gemini analyses are large (base64 thumbnails)
/eval/results/*.analysis.json
!/eval/results/smoke-test.analysis.json

# local game footage for evals — never commit video files
/eval/videos/
# cached possession clips for the outcome bench — local only
/eval/clips/

.vercel
*.tsbuildinfo
next-env.d.ts
